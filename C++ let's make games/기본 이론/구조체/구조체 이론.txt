
/*구조체 : 관련 있는 변수들을 모아서 하나의 새로운 타입들을 만들어주는 기능.
사용자정의 변수타입이다.
형태 : struct 구조체명 {}; */

#include <iostream>

using namespace std;
#define NAME_SIZE 32 // 여기를 통해서 구조체의 배열의 길이를 통일할 수 있다. 

struct _tagStudent {
	int iKor;
	int iEng;
	int iNumber;
	int iMath;
	int iTotal; //4byte X5 = 20byte
	float fAVg; //4byte 
	char strName[NAME_SIZE]; // 32byte 배열의 길이, 몇 글자를 넣을 거냐?
};
//총 52byte을 잡아놓고 하나인 것처럼 분할하여 실행.
int main() {

	_tagStudent tStudent = {};
	_tagStudent tStudentArr[100] = {};

	//구조체 멤버에 접근할 떄는 . 을 이용해서 접근하게 된다.
	tStudent.iKor = 100;
	strcpy_s(tStudent.strName, "가나다라 asdf"); //strcpy_s 함수는 문자열을 붙임. 
	strcpy_s(tStudent.strName, "문자열 붙이기"); // 오른쪽에 있는 문자열을 왼쪽에 붙임
	 //strName는 위에서 가나다랄을 저장했기에 오른쪽 문자열을 붙여서 만들어준다.	
	
	 //strcmp 함수는 두 문자열을 비교하여 같으면 0, 아니면 0이 아닌 값을 반환.
	strcpy_s(tStudent.strName, "홍길동");

	cout << "비교할 이름을 입력하세요 : ";
	char  strName[NAME_SIZE] = {};
	cin >> strName;

	if (strcmp(tStudent.strName, strName) == 0)
	{
		cout << "학생을 찾았습니다." << endl;
	}
	else
		cout << "찾는 학생이 없습니다." << endl;

	//strtok.  특정한 문자를 임의로 정하고, 문제를 해결한다.  
	cout << "이름 길이 : " << strlen(tStudent.strName) << endl;
	
	/*
	//문자열의 끝은 항상 0으로 끝이 나며, 쓰레기값이 들어간 상태에서는 아래처럼
	//각 배열 요소에 값을 넣어주면 그 값이 출력되고, 안 넣어준 부분에서는 쓰레기값이 그대로 출력된다.
	//왜냐하면 출력할 때 문자열의 끝을 인식할 수 없으며, 0이 들어가야 하는데, 쓰레기값이 들어가기도 하기 때문이다. 

	//tStudent.strName[0] = 'a';
	//tStudent.strName[1] = 'b';
	//tStudent.strName[2] = 0 ; 
	cout << "비교할 이름을 입력하세요 : ";
	cout << "이름 : " << tStudent.strName << endl;
	cout << "학번 : " << tStudent.iNumber << endl;
	cout << "국어 : " << tStudent.iKor << endl;
	cout << "영어 : " << tStudent.iEng << endl;
	cout << "수학 : " << tStudent.iMath << endl;
	cout << "총점 : " << tStudent.iTotal << endl;
	cout << "평균 :" << tStudent.fAVg << endl;
	//tStudent.strName = "aslkdjfwi가";
	//문자열을 배열에 넣어줄 떄에는 단순 대입으로는 불가능하다.
	//strcpy_s라는 함수를 이용해서 문자열을 복사해 주어야 한다.
	*/
	return 0;
}

//배열과 구조체의 공통점 : 
// 1. 데이터의 집합이다. 
// 2. 연속된 메모리 블럭에 할당된다. 구조체 멤버들은 연속된 블럭으로 할당.

