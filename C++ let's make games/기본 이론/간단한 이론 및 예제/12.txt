//12화, 배열
		/*
		do ~ while 문 (반복문의 한 종류) , 형태 : do {}while(조건식).
		while문은 처음 진입부터 조건식을 체크하지만, do while은 처음 한번은 무조건 동작이 되고,
		그 후에 조건식을 체크해서 true일 경우 동작하는 반복문.
		*/

		//int iNumber = 0;
		//int iNumber1, iNumber2, iNumber3;

		//do
		//{
		//	cout << iNumber <<endl;
		//} while (iNumber > 0);

		/*
		배열 : 여러개의 변수를 한번에 생성해줄 수 있는 기능.
		형태 : 변수타입 배열명[개수].
		배열의 특징 : 배열은 연속된 메모리 블럭에 공간이 할당된다. 인덱스를 활용하여 원하는 부분에 접근하여 값을 산정.
		인덱스는 0부터 개수-1까지.(if, 10개이면 0 ~ 9 까지 총 10개의 인덱스)
		*/
		//int iArray[10] = { 1,2,3,4,5,6,7,8,9 };
	int iArray[10] = { 1,2,3,4,5 }; // 비어있는 중괄호를 대입할 경우 모든 인덱스를 0으로 초기화한다.

	iArray[1] = 1234;

	for (int i = 0; i < 10; ++i)
	{
		cout << iArray[i] << endl;
		//배열 개수를 2개 시정하면 이차원배열이 된다. 3개 시정하면 3차원 배열이 된다.
		//2차원 배열의 개수는 앞의 수 * 뒤의 수만큼 차이가 나게 된다. 
		int iArray2[10][10] = { {1,2,3}, {4,5,6} };

		for (int i = 0; i < 10; ++i)
		{
			for (int j = 0; j < 10; ++j)
			{
				cout << iArray2[i][j] << "Wt";
			}
			cout << endl;
		} //int iArr[99999999999999];

		system("cls");

		srand((unsigned int)time(0));

		//Lotto
		int iLotto[45] = {};

		for (int i = 0; i < 45; ++i)
		{
			iLotto[i] = i + 1;
		}//1~45까지의 숫자를 차례대로 넣어준다. 

		//Swap 알고리즘
		/**int iNum1 = 1, iNum2 = 2, iNum3;
		iNum3 = iNum1;
		iNum1 = iNum2;
		iNum2 = iNum3; */

		//shuffle
		int iTemp, idx1, idx2;//100번 정도 골고루 섞는다.
		for (int i = 0; i < 100; ++i)
		{
			idx1 = rand() % 45;
			idx2 = rand() % 45;

			iTemp = iLotto[idx1];
			iLotto[idx1] = iLotto[idx2];
			iLotto[idx2] = iTemp;
		}

		for (int i = 0; i < 45; ++i)
		{
			cout << iLotto[i] << endl;
		}
	
	return 0;
	}



