#include <iostream> //전처리기.
#include <time.h> //9강
using namespace std;

int main() // C++ 의 시작점, 무조건 있어야 함.
{
	//1강
	//Ctrl + Shift + B 을 이용해서 컴파일, 빌드를 할 수 있다. 
	// C++ 표준 기능 대부분은 std라는 namespace 안에 존재. 이름이 겹치는 것을 방지.
	// cout : 콘솔창에 출력해주는 기능. 뒤에 있는 " "(큰따옴표)안에 있는 문자들을 화면에 출력.

	std::cout << "Test OutPut" << std::endl;
	std::cout << "가나다라" << std::endl;

	cout << "std namespace 사용" << endl;

}

	