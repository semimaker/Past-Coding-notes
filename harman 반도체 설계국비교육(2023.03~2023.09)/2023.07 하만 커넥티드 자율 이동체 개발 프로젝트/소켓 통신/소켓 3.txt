예제 2: 센서 데이터 수신
csharp
Copy code
using System;
using System.Net;
using System.Net.Sockets;
using System.Text;
using System.Threading;

public class AutonomousCarController
{
    private Socket socket;
    private const int port = 8080;

    public AutonomousCarController()
    {
        socket = new Socket(AddressFamily.InterNetwork, SocketType.Stream, ProtocolType.Tcp);
    }

    public void ConnectToSmartphone(string ipAddress)
    {
        try
        {
            IPAddress serverIP = IPAddress.Parse(ipAddress);
            IPEndPoint serverEndPoint = new IPEndPoint(serverIP, port);
            socket.Connect(serverEndPoint);
            Console.WriteLine("Connected to smartphone at: " + socket.RemoteEndPoint);
        }
        catch (Exception e)
        {
            Console.WriteLine("Failed to connect to smartphone: " + e.Message);
        }
    }

    public void StartSensorDataTransmission()
    {
        Thread sensorThread = new Thread(SendSensorData);
        sensorThread.Start();
    }

    private void SendSensorData()
    {
        // 센서 데이터 수집 및 전송 코드를 구현하세요.
        // 예를 들면, 자동차의 속도, 거리 센서 등의 데이터를 읽고
        // 해당 데이터를 소켓을 통해 스마트폰으로 전송합니다.
        while (true)
        {
            // 센서 데이터를 수집하고 전송하는 로직을 여기에 추가하세요.
        }
    }
    
    // ...
    // 다른 메서드들은 이전 예제와 동일하게 유지합니다.
}
이 예제 코드는 센서 데이터를 수집하고 스마트폰으로 전송하는 기능을 추가한 버전입니다. StartSensorDataTransmission 메서드를 호출하면 센서 데이터를 수집하고 전송하는 쓰레드가 시작됩니다. SendSensorData 메서드에서는 자동차의 속도, 거리 센서 등의 데이터를 읽고 해당 데이터를 소켓을 통해 스마트폰으로 전송하는 로직을 구현해야 합니다.

이 예제 코드들은 C#을 사용하여 자율주행 자동차를 스마트폰으로 조종하기 위한 다양한 기능을 추가하는 방법을 보여줍니다. 원하는 동작 및 기능을 추가하고, 코드를 수정하여 자신의 요구사항에 맞게 확장할 수 있습니다.